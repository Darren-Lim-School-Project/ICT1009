spring:
  # redis:
  #   port: 6379
  #   host: localhost
  session:
    # or redis
    store-type: none
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/kwsm
    username:
    password:
